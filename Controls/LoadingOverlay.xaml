<UserControl x:Class="ContribCalendar.Controls.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ContribCalendar.Controls"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="200"
             Background="Transparent">
    <Border Background="#80000000" CornerRadius="8" Padding="20">
        <StackPanel>
            <!-- Loading 动画 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Ellipse Width="12" Height="12" Fill="#4CAF50" Margin="4">
                    <Ellipse.RenderTransform>
                        <ScaleTransform x:Name="Scale1" ScaleX="0" ScaleY="0"/>
                    </Ellipse.RenderTransform>
                    <Ellipse.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetName="Scale1"
                                                   Storyboard.TargetProperty="ScaleX"
                                                   From="0" To="1" Duration="0:0:0.4"
                                                   AutoReverse="True">
                                        <DoubleAnimation.EasingFunction>
                                            <SineEase EasingMode="EaseInOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetName="Scale1"
                                                   Storyboard.TargetProperty="ScaleY"
                                                   From="0" To="1" Duration="0:0:0.4"
                                                   AutoReverse="True">
                                        <DoubleAnimation.EasingFunction>
                                            <SineEase EasingMode="EaseInOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Ellipse.Triggers>
                </Ellipse>

                <Ellipse Width="12" Height="12" Fill="#4CAF50" Margin="4">
                    <Ellipse.RenderTransform>
                        <ScaleTransform x:Name="Scale2" ScaleX="0" ScaleY="0"/>
                    </Ellipse.RenderTransform>
                    <Ellipse.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever" BeginTime="0:0:0.1">
                                    <DoubleAnimation Storyboard.TargetName="Scale2"
                                                   Storyboard.TargetProperty="ScaleX"
                                                   From="0" To="1" Duration="0:0:0.4"
                                                   AutoReverse="True">
                                        <DoubleAnimation.EasingFunction>
                                            <SineEase EasingMode="EaseInOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetName="Scale2"
                                                   Storyboard.TargetProperty="ScaleY"
                                                   From="0" To="1" Duration="0:0:0.4"
                                                   AutoReverse="True">
                                        <DoubleAnimation.EasingFunction>
                                            <SineEase EasingMode="EaseInOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Ellipse.Triggers>
                </Ellipse>

                <Ellipse Width="12" Height="12" Fill="#4CAF50" Margin="4">
                    <Ellipse.RenderTransform>
                        <ScaleTransform x:Name="Scale3" ScaleX="0" ScaleY="0"/>
                    </Ellipse.RenderTransform>
                    <Ellipse.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever" BeginTime="0:0:0.2">
                                    <DoubleAnimation Storyboard.TargetName="Scale3"
                                                   Storyboard.TargetProperty="ScaleX"
                                                   From="0" To="1" Duration="0:0:0.4"
                                                   AutoReverse="True">
                                        <DoubleAnimation.EasingFunction>
                                            <SineEase EasingMode="EaseInOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetName="Scale3"
                                                   Storyboard.TargetProperty="ScaleY"
                                                   From="0" To="1" Duration="0:0:0.4"
                                                   AutoReverse="True">
                                        <DoubleAnimation.EasingFunction>
                                            <SineEase EasingMode="EaseInOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Ellipse.Triggers>
                </Ellipse>
            </StackPanel>

            <!-- Loading 文本 -->
            <TextBlock Text="扫描中..."
                       FontSize="14"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0"/>
        </StackPanel>
    </Border>
</UserControl>
